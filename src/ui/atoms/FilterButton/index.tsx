import React from "react";
import { FilterButtonContainer } from "./styles";
import FilterIcon from '../../../assets/FilterComponent'

const FilterButton = () => {

    return(
        <FilterButtonContainer>
            <FilterIcon />
        </FilterButtonContainer>

    );
};

export default FilterButton